apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: integrationinstances.integrations.jupiterone.io
spec:
  group: integrations.jupiterone.io
  names:
    kind: IntegrationInstance
    listKind: IntegrationInstanceList
    plural: integrationinstances
    singular: integrationinstance
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=="Ready")].status
      name: Ready
      type: string
    - jsonPath: .status.conditions[?(@.type=="Ready")].reason
      name: Reason
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1
    schema:
      openAPIV3Schema:
        description: IntegrationInstance is the Schema for the integrationinstances
          API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: IntegrationInstanceSpec defines the desired state of IntegrationInstance.
            properties:
              collectorName:
                description: |-
                  Important: Run "make" to regenerate code after modifying this file
                  CollectorName is the name of the collector to use for this integration instance
                type: string
              config:
                additionalProperties:
                  type: string
                description: Config to pass to the create integration API (non-sensitive
                  configuration)
                type: object
              integrationDefinitionName:
                description: IntegrationDefinitionName is the name of the integration
                  which will map to an integration definition id
                type: string
              integrationInstanceID:
                description: IntegrationInstanceID is the ID of the created integration
                  instance in JupiterOne
                type: string
              pollingInterval:
                description: PollingInterval defines how often the integration should
                  poll for data
                type: string
              pollingIntervalCron:
                description: PollingIntervalCron defines a cron schedule for polling
                  the integration
                properties:
                  dayOfWeek:
                    type: integer
                  hour:
                    type: integer
                required:
                - dayOfWeek
                - hour
                type: object
              resourceGroupID:
                description: ResourceGroupID is the ID of the resource group in JupiterOne
                type: string
              secretRef:
                description: |-
                  SecretRef is the name of a Secret containing sensitive configuration (e.g., credentials).
                  The secret data will be merged with Config when creating/updating the integration instance.
                  Secret keys will override Config keys if there are conflicts.
                type: string
            type: object
          status:
            description: IntegrationInstanceStatus defines the observed state of IntegrationInstance.
            properties:
              conditions:
                description: |-
                  INSERT ADDITIONAL STATUS FIELD - define observed state of cluster
                  Important: Run "make" to regenerate code after modifying this file
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
