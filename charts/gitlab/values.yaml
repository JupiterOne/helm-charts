# This file was auto-generated by chartgen. Do not edit manually.

# The name of the collector (a.k.a IntegrationRunner) in the same namespace
collectorName: runner

# Polling interval defines how often the integration should run. Options are:
# DISABLED
# THIRTY_MINUTES
# ONE_HOUR
# FOUR_HOURS
# EIGHT_HOURS
# TWELVE_HOURS
# ONE_DAY
# ONE_WEEK
pollingInterval: "ONE_WEEK"

# Polling interval cron schedule (instead of pollingInterval)
# pollingIntervalCron:
#   hour: 2          # Hour of the day (0-23)
#   dayOfWeek: 0     # Day of the week (0-6)

# Resource Group ID to associate with the integration instance
# resourceGroupId: "your-resource-group-id"

# Name of the Secret containing sensitive configuration (credentials, API keys, etc.)
# If createSecret is true, this secret will be created by the chart.
# If createSecret is false, you must create this secret externally.
secretName: "gitlab-secret"

# Whether to create the secret for sensitive configuration
# Set to false if you want to manage the secret externally
createSecret: true

# =============================================================================
# Integration Configuration
# =============================================================================

# The GitLab API base URL.
# Example: https://gitlab.com or your self-managed instance URL.
baseUrl:

# Select vulnerability severities to ingest
# Options: info, low, medium, high, critical
# vulnerabilitySeverities:

# Select vulnerability states to ingest
# Options: confirmed, detected, dismissed, resolved
# vulnerabilityStates:

# Select vulnerability report types to ingest
# Options: api_fuzzing, cluster_image_scanning, container_scanning, container_scanning_for_registry, dast, dependency_scanning, generic, sast, secret_detection
# vulnerabilityReportTypes:

# Specify the ingestion window for updated merge requests (days ago)
# Options: 90, 180, 275, 365
# mergeRequestIngestSinceDays: "90"

# When enabled, the 'Fetch Users' step will skip all bot accounts, including project and group bots
# ingestRegularUsersOnly:

# Is the gitlab instance self-managed
# isSelfManaged:

# Use the project vulnerabilities query instead of the group vulnerabilities query
# useProjectVulnerabilitiesQuery:

# Skip projects that are marked for deletion or archived.
# skipDisabledProjects:

# When set to true, the query skips filtering by user membership and retrieves all projects visible to the authenticated user instead of only those where the user is a direct member.
# disableProjectMembershipFetch:

# =============================================================================
# Sensitive Configuration (stored in Secret)
# Only used when createSecret is true
# =============================================================================
secret:

  # ---------------------------------------------------------------------------
  # Credentials
  # ---------------------------------------------------------------------------

  # An access token configured for read access
  personalToken:

  # Optional Access token for findings added for known issue: https://gitlab.com/gitlab-org/gitlab/-/issues/542815.
  # findingsIngestionToken:
