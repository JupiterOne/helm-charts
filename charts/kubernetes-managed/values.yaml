# This file was auto-generated by chartgen. Do not edit manually.

# The name of the collector (a.k.a IntegrationRunner) in the same namespace
collectorName: runner

# Polling interval defines how often the integration should run. Options are:
# DISABLED
# THIRTY_MINUTES
# ONE_HOUR
# FOUR_HOURS
# EIGHT_HOURS
# TWELVE_HOURS
# ONE_DAY
# ONE_WEEK
pollingInterval: "ONE_WEEK"

# Polling interval cron schedule (instead of pollingInterval)
# pollingIntervalCron:
#   hour: 2          # Hour of the day (0-23)
#   dayOfWeek: 0     # Day of the week (0-6)

# Resource Group ID to associate with the integration instance
# resourceGroupId: "your-resource-group-id"

# =============================================================================
# Integration Configuration
# =============================================================================

# Specify the namespaces you want to scan. Only the namespaces listed here will be ingested. If this field is left empty, all namespaces will be scanned (unless you specify exclusions below). Separate namespace names with commas.
# Example: default,production,staging
# includeNamespaces:

# Specify the namespaces you want to exclude from scanning. Any namespace listed here will be skipped during ingestion. This is useful for excluding system namespaces or namespaces you don't want to monitor. Separate namespace names with commas.
# Example: kube-system,kube-public,kube-node-lease
# excludeNamespaces:

# Enable this if you're running the integration outside of Kubernetes (like on your local machine). Disable this if you're running the integration inside a Kubernetes cluster. When disabled, the integration will use the cluster's built-in authentication.
# loadKubernetesConfigFromDefault:

# Specify in YAML format the field mapping between Kubernetes Custom Resource Definitions and JupiterOne entities.
# crdMappingConfig:
