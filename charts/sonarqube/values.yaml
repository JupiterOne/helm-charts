# This file was auto-generated by chartgen. Do not edit manually.

# The name of the collector (a.k.a IntegrationRunner) in the same namespace
collectorName: runner

# Polling interval defines how often the integration should run. Options are:
# DISABLED
# THIRTY_MINUTES
# ONE_HOUR
# FOUR_HOURS
# EIGHT_HOURS
# TWELVE_HOURS
# ONE_DAY
# ONE_WEEK
pollingInterval: "ONE_WEEK"

# Polling interval cron schedule (instead of pollingInterval)
# pollingIntervalCron:
#   hour: 2          # Hour of the day (0-23)
#   dayOfWeek: 0     # Day of the week (0-6)

# Resource Group ID to associate with the integration instance
# resourceGroupId: "your-resource-group-id"

# Name of the Secret containing sensitive configuration (credentials, API keys, etc.)
# If createSecret is true, this secret will be created by the chart.
# If createSecret is false, you must create this secret externally.
secretName: "sonarqube-secret"

# Whether to create the secret for sensitive configuration
# Set to false if you want to manage the secret externally
createSecret: true

# =============================================================================
# Integration Configuration
# =============================================================================

# The base url of your SonarQube instance.
# Example URL: https://example.com
baseUrl:

# Limit the ingestion of findings based on specified severities. You can specify one or more severities. The values in parentheses correspond to severities in SonarQube version 10.4 and above.
# Options: INFO, MINOR, MAJOR, CRITICAL, BLOCKER
# findingSeverities:

# Limit the ingestion of findings based on specified statuses. You can specify one or more statuses. The values in parentheses correspond to statuses in SonarQube version 10.4 and above.
# Options: OPEN, CONFIRMED, REOPENED, RESOLVED, CLOSED
# findingStatus:

# Limit the ingestion of findings based on specified types. You can specify one or more types. The values in parentheses correspond to statuses in SonarQube version 10.4 and above.
# Options: CODE_SMELL, BUG, VULNERABILITY
# findingTypes:

# Specify the ingestion window for created findings (days ago)
# Options: 90, 180, 275, 365
# findingsIngestSinceDays: "90"

# =============================================================================
# Sensitive Configuration (stored in Secret)
# Only used when createSecret is true
# =============================================================================
secret:

  # ---------------------------------------------------------------------------
  # Credentials
  # ---------------------------------------------------------------------------

  # The API token used to authenticate with SonarQube.
  apiToken:
