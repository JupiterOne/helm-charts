{{if .Values.createSecret}}
apiVersion: v1
kind: Secret
metadata:
  name: {{ .Values.secretName }}
  namespace: {{ .Values.namespace }}
type: Opaque
data:
  # This is obtained after creating a collector in the JupiterOne UI.
  auth-token: {{ .Values.authToken | b64enc }}
{{- end }}