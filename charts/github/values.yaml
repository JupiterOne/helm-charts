# This file was auto-generated by chartgen. Do not edit manually.

# The name of the collector (a.k.a IntegrationRunner) in the same namespace
collectorName: runner

# Polling interval defines how often the integration should run. Options are:
# DISABLED
# THIRTY_MINUTES
# ONE_HOUR
# FOUR_HOURS
# EIGHT_HOURS
# TWELVE_HOURS
# ONE_DAY
# ONE_WEEK
pollingInterval: "ONE_WEEK"

# Polling interval cron schedule (instead of pollingInterval)
# pollingIntervalCron:
#   hour: 2          # Hour of the day (0-23)
#   dayOfWeek: 0     # Day of the week (0-6)

# Resource Group ID to associate with the integration instance
# resourceGroupId: "your-resource-group-id"

# Name of the Secret containing sensitive configuration (credentials, API keys, etc.)
# If createSecret is true, this secret will be created by the chart.
# If createSecret is false, you must create this secret externally.
secretName: "github-secret"

# Whether to create the secret for sensitive configuration
# Set to false if you want to manage the secret externally
createSecret: true

# =============================================================================
# Integration Configuration
# =============================================================================

# Auto-generated GitHub application ID
# NOTE: We do not recommend updating this value unless you've installed a new JupiterOne application into your GitHub organization.
# installationId:

# Specify the ingestion window for updated pull requests (days ago)
# Options: 90, 180, 275, 365
# pullRequestIngestSinceDays: "90"

# Specify the ingestion window for updated issues (days ago)
# Options: 90, 180, 275, 365
# issuesIngestSinceDays: "90"

# The batch size to use for the Fetch Collaborators step.
# collaboratorsBatchSize:

# The maximum number of repositories to search for in a single request.
# repositoryMaxSearchLimit:

# Limit ingestion to alerts with the specified alert states. One or more states may be specified. Not supported on GHE Server versions <3.5
# Options: OPEN, DISMISSED, FIXED
# dependabotAlertStates:

# Limit ingestion to alerts with the specified severity. One or more severities may be specified.
# Options: CRITICAL, HIGH, MODERATE, LOW
# dependabotAlertSeverities:

# Ingest generic secret alerts. This will include default secret patterns and generic patterns.
# ingestGenericSecretAlerts:

# =============================================================================
# Sensitive Configuration (stored in Secret)
# Only used when createSecret is true
# =============================================================================
secret:

  # ---------------------------------------------------------------------------
  # Github Cloud (App) - Recommended
  # ---------------------------------------------------------------------------
  # selectedAuthType: "githubCloud"

  # ---------------------------------------------------------------------------
  # Github Cloud (PAT)
  # ---------------------------------------------------------------------------
  # selectedAuthType: "githubEnterpriseToken"

  # The enterprise slug (the short identifier for your enterprise account in the URL https://github.com/enterprises/<your slug>)
  # enterpriseSlug:

  # The name of the organization from which to ingest data. It can be found in the URL when you navigate to your organization's page (https://github.com/<your organization name>).
  # organization:

  # The Enterprise Owner's Personal Access Token (classic) to use for authentication.
  # enterpriseToken:

  # ---------------------------------------------------------------------------
  # Github Enterprise Server
  # ---------------------------------------------------------------------------
  # selectedAuthType: "githubEnterpriseServer"

  # The URL for your hosted GitHub Enterprise Server.
  # githubApiBaseUrl:

  # Numerical ID of the App created in your GitHub Enterprise Server.
  # githubAppId:

  # Numerical ID of the installed app.
  # githubInstallationId:

  # Allows JupiterOne to access your GitHub Enterprise Server APIs.
  # githubAppPrivateKey:
