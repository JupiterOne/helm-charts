# This file was auto-generated by chartgen. Do not edit manually.

# The name of the collector (a.k.a IntegrationRunner) in the same namespace
collectorName: runner

# Polling interval defines how often the integration should run. Options are:
# DISABLED
# THIRTY_MINUTES
# ONE_HOUR
# FOUR_HOURS
# EIGHT_HOURS
# TWELVE_HOURS
# ONE_DAY
# ONE_WEEK
pollingInterval: "ONE_WEEK"

# Polling interval cron schedule (instead of pollingInterval)
# pollingIntervalCron:
#   hour: 2          # Hour of the day (0-23)
#   dayOfWeek: 0     # Day of the week (0-6)

# Resource Group ID to associate with the integration instance
# resourceGroupId: "your-resource-group-id"

# Name of the Secret containing sensitive configuration (credentials, API keys, etc.)
# If createSecret is true, this secret will be created by the chart.
# If createSecret is false, you must create this secret externally.
secretName: "manage-engine-ec-secret"

# Whether to create the secret for sensitive configuration
# Set to false if you want to manage the secret externally
createSecret: true

# =============================================================================
# Sensitive Configuration (stored in Secret)
# Only used when createSecret is true
# =============================================================================
secret:

  # ---------------------------------------------------------------------------
  # Cloud
  # ---------------------------------------------------------------------------
  # selectedAuthType: "cloud"

  # Endpoint Central API endpoint
  # For Cloud Authentication, you can consult the correct domain in here: https://www.manageengine.com/products/desktop-central/api/cloud_index.html#api-structure. For on premises, the Server URL.
  # endpointCentralEndpoint:

  # Zoho Account API endpoint
  # You can consult the correct domain in here: https://www.manageengine.com/products/desktop-central/api/cloud_index.html#api-structure
  # zohoAccountEndpoint:

  # The Zoho application Client ID
  # The client id from the Zoho API Console
  # zohoClientId:

  # The Zoho application Client Secret
  # The client secret from the Zoho API Console
  # zohoClientSecret:

  # The Refresh Token generate from the oauth token request
  # Follow the docs to generate a refresh token
  # zohoRefreshToken:

  # ---------------------------------------------------------------------------
  # On-Premise Local Authentication
  # ---------------------------------------------------------------------------
  # selectedAuthType: "onPremLocal"

  # Endpoint Central API endpoint
  # endpointCentralEndpoint:

  # Username for the on-premise endpoint
  # onPremUsername:

  # Password for the on-premise endpoint
  # onPremPassword:

  # ---------------------------------------------------------------------------
  # On-Premise AD Authentication
  # ---------------------------------------------------------------------------
  # selectedAuthType: "onPremAD"

  # Endpoint Central API endpoint
  # endpointCentralEndpoint:

  # Username for the on-premise endpoint
  # onPremUsername:

  # Password for the on-premise endpoint
  # onPremPassword:

  # AD authentication enabled domain
  # domainName:

  # ---------------------------------------------------------------------------
  # On-Premise Token Authentication
  # ---------------------------------------------------------------------------
  # selectedAuthType: "onPremToken"

  # Endpoint Central API endpoint
  # endpointCentralEndpoint:

  # The generated API token. Valid unless the user resets their password, is removed, or an expiry is set during key generation.
  # onPremAPIToken:
